{
  "name": "taranaapp",
  "version": "1.0.0",
  "proxy": "http://localhost:3001",
  "author": "",
  "license": "ISC",
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "concurrently \"cd frontend && npm install && npm start\" \"cd backend && npm install && node server.js\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
